<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zh.look.mapper.UsersMapper">

    <select id="getUserRoles" resultType="com.zh.look.dao.UserRoles">
        select * from User_Roles AS ur where user_id = #{userId}
    </select>
</mapper>
